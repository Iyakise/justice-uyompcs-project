
/**MEMBER IS TRYING TRANSFER TO OTHER MEMBER
 * FROM MEMBER TO MEMBER
 * FROM MEMBER TO SELF
 */
setInterval(__memberNotification__, 3000)
function __memberNotification__(){
    var itemGet, a, b, c, d, xhr, rtnNotification;
        itemGet = document.querySelector('.Usr-rtn');
            a = notificationData.getAttribute('mpcmemberphone');
            b = notificationData.getAttribute('mpcmemberid');
        rtnNotification = document.querySelector('.subNoti');


//START TO PROCESS AJAX REQUEST HERE

        xhr = new XMLHttpRequest(); //start ajax request here
        xhr.open('GET', __mpc_uri__() + 'functions/mpc-mem-ajax.php?X=MEMpErM&MEMpH=' + a +'&MEMId=' + b, true);
        xhr.onreadystatechange = function(){
            if(this.readyState == 4 && this.status == 200){
                // i stop here
                rtnNotification.innerHTML = this.response;
                rtnNotification.title = "You have "+ this.response + ' New Notification';
            }
        }
        xhr.send();
            
}

window.onload = function(){
   // __memberNotification__()
}